
<!DOCTYPE html>
<html>
<head>
	<title>Yelp Camp</title>
	<link rel="stylesheet" type="text/css" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
  <link rel="stylesheet" type="text/css" href="/stylesheets/main.css">
</head>
<body>
	<nav class="navbar navbar-expand-lg navbar-light bg-light justify-content-between">
		<div class="container-fluid">
			<div class="navbar-header">
				<a class="navbar-brand" href="/">YelpCamp</a>
			</div>
			<div class="collapse navbar-collapse">
				<ul class="navbar-nav ml-auto">
          <% if(!currentUser) {%>
					   <li style="margin-right: 1em;" class="btn btn-outline-success my-2 my-sm-0"><a href="/login">Login</a></li>
					   <li style="margin-right: 1em;" class="btn btn-outline-success my-2 my-sm-0"><a href="/register">Sign Up</a></li>
          <% }else { %>
              <li class="btn my-2 my-sm-0"><a href="#"> Signed In As <%= currentUser.username%></a></li>
              <li class="btn btn-outline-success my-2 my-sm-0"><a href="/logout">Logout</a></li> 
          <% } %>
				</ul>
			</div>
		</div>
	</nav>

<div class="container">
	<% if(error && error.length > 0){ %>
		<div class="alert alert-danger" role="alert">
			<h4><%=error%></h4>
		</div>
<% } %>
	<% if(success && success.length > 0){ %>
		<div class="alert alert-success" role="alert">
			<h4><%=success%></h4>
		</div>
<% } %>
</div>



